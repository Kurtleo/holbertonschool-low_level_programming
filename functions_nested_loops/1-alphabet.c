#include "main.h"

/**
 * main - prints the alphabet in lowercase
 *
 * Return: void
 */
void print_alphabet(void);
{
	char (A);

	for (A = a; a <= z;)
	{
	putchar (A) A++;
	}
	putchar ('\n');
	return (0);

}
