#include <stdio.h>

/**
 * main - prints all numbers of base 16 in lowercase
 *
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 9; n++)
	putchar(n  + '0');
	putchar('\n');

	return (0);

}
