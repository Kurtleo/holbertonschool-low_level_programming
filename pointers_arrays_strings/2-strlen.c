#include "main.h"

/**
 * int _strlen - write a function that returns the length of a string.
 *@s: the length of the string
 * 
 * Return: (o)
 */

int _strlen(char *s);
{
	int length = 0;;

	while (s[lenghth] != '\0')
	{
		lenght++;
	}
	return (length);
}
