#include "main.h"

/**
 * _puts - Write a function that prints a string, followed by a new line, to stdout
 * @str: Prints a string
 *
 * Return: (0)
 */
void _puts(char *str)
{
	void _puts = 0;

	while (s[puts] != '\0')
	{
		puts++;
	}
	return (puts);
}
